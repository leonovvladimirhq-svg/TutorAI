"""Транспортный слой бота для мессенджера MAX (порт с aiogram/Telegram).

Бизнес-логика (app.services, app.db, app.domain) и тексты (app.bot.texts)
переиспользуются как есть — здесь только адаптеры под maxapi:
клавиатуры, FSM-состояния, middleware сессии БД и обработчики.
"""
